// which service it is
describe('Shopping Service', () => {
  // which function
  describe('PlaceOrder', () => {
    // which test case
    test('validate user inputs', () => {})

    test('validate response', async () => {})
  })
})
